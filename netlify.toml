[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Tell Netlify's secret scanner to specifically ignore these public keys
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_PUBLISHABLE_KEY,VITE_SUPABASE_URL"
